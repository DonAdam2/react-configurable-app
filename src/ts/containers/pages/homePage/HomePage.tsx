import React, { FC } from 'react';
// import PropTypes from 'prop-types';
// styles
import classes from './HomePage.scss';

const HomePage: FC = (): JSX.Element => {
	return <div className={classes.homeWrapper}>HomePage</div>;
};

HomePage.propTypes = {};

export default HomePage;
