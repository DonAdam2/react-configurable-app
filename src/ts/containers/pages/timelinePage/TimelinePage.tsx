import React, { FC } from 'react';
// styles
import classes from './TimelinePage.scss';

const TimelinePage: FC = (): JSX.Element => {
	return <div className={classes.timelineWrapper}>TimelinePage</div>;
};

export default TimelinePage;
